server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/appointmentdb
    username: root
    password: tiger

  jpa:
    hibernate:
      ddl-auto: update

  rabbitmq:
    host: localhost
    port: 5672

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
